# Frontend environment variables (example)
# Copy this file to .env or .env.local in this same folder and adjust values as needed.
# Note: Do NOT commit your real .env. Use this example as a template.

# Local development
# The backend server (Express) running locally
VITE_API_URL=http://localhost:4000/api

# Production example (replace with your actual API domain)
# VITE_API_URL=https://real-estate-management-system-dk081xb3h.vercel.app/api
